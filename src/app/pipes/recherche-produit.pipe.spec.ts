import { RechercheProduitPipe } from './recherche-produit.pipe';

describe('RechercheProduitPipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheProduitPipe();
    expect(pipe).toBeTruthy();
  });
});
